# M2 微正则系综（孤立系统）

> **核心目标**：虽然在现实中很少处理完全孤立的系统，但**微正则**（Microcanonical）是所有统计力学的自洽性基石。我们需要在这里把两条基准公式打牢，这是后续所有系综的“参照系”。

## 基准公式（M2 的参照系）
\[
S(E,V,N)=k_B \ln \Omega(E,V,N)
\]
\[
\frac{1}{T}=\left(\frac{\partial S}{\partial E}\right)_{V,N}
\]

## 推荐学习顺序
1. 通读 Part 1–4，理解为什么我们不需要证明“各态历经”也能做统计物理。
2. 做 `exercises/written/M2_microcanonical_basics.md`，完成两个计数例子（两盒分球 + 理想气体相空间体积），并从 \(\ln\Omega\) 的偏导推出 \(PV=Nk_BT\)。
3. 重点理解 Part 3 的“典型性”，这是大系统宏观规律确定的根本原因。

## 背景最小包（First Principles）
- 这是“硬约束”的极限：只知道 \(E,V,N\) 就在满足约束的集合里计数。
- 关键不是系统是否遍历全部微观态，而是**大数下的典型性**让宏观量稳定。
- 一旦 \(S(E,V,N)\) 可定义，\(T,P,\mu\) 就自然作为偏导出现。

## 逻辑桥接（M1 → M2）
- M1 给出“在约束下最合理的分布”；当约束是硬的，最合理的分布就是等概率计数。
- M2 把“计数”变成热力学偏导，从而解释温度与压强的统计含义。

---

## Introduction

统计物理要解释一个看似矛盾、但你每天都在依赖的事实：含 \(10^{23}\) 个自由度的系统，微观态每时每刻都在剧烈变化，但宏观量（压强、温度、密度）却几乎不抖。**这种“微观混乱 + 宏观确定”的稳定性从何而来？**

在 M0，我们把热力学当作一门“记账语言”：写下 \(dU = T\,dS - P\,dV + \mu\,dN\)，并把 \(T,P,\mu\) 当作偏导定义。但这套语法背后仍有两个关键空缺：  
1) 为什么这些偏导对应可测的温度/压强/化学势？  
2) 为什么宏观量在巨大自由度下看起来几乎不抖？

微正则系综从最少假设出发补上这两个空缺：把系统视作**孤立**的，只施加硬约束 \((E,V,N)\)，并在满足约束的可及微观态之间等概率取样（见：[[微正则系综 Microcanonical ensemble]]）。在这个测度下，多重度 \(\Omega(E,V,N)\) 成为一阶对象，Boltzmann 熵定义为 \(S(E,V,N)=k_B\ln\Omega\)（见：[[多重度 multiplicity Ω]]、[[熵 Entropy]]）。

接着，只要让两个系统允许交换能量/体积/粒子，平衡态就是总熵 \(S_{\text{tot}}\) 的极大点；这一步会“强迫”强度量以偏导形式出现：
\[
\frac{1}{T}\equiv\left(\frac{\partial S}{\partial E}\right)_{V,N},\quad
\frac{P}{T}\equiv\left(\frac{\partial S}{\partial V}\right)_{E,N},\quad
-\frac{\mu}{T}\equiv\left(\frac{\partial S}{\partial N}\right)_{E,V}.
\]
（推导见：[[T,P,μ 作为熵的偏导定义]]。）

宏观确定性则来自热力学极限下的典型性：\(\Omega\) 往往随 \(N\) 指数增长（所以 \(S\sim N\)），使得极大点附近的概率分布极端尖锐，相对涨落通常按 \(1/\sqrt{N}\) 消失（见：[[热力学极限 Thermodynamic limit]]）。因此我们不必先证明“各态历经”才能做统计物理：在大系统里，“几乎所有”微观态本来就给出同一个宏观值。

城市镜像：微正则提供“硬约束下的封闭基线”（只知道总预算/总资源/总人数时，计数会把系统推向最概然宏观态）；而真实城市通常开放且受驱动，后续 M3/M4 会把硬约束转写成更可操作的软约束（热库/粒子库），以便建模与计算。

## References
- **Khinchin 1949**: *Mathematical Foundations*. 导读见 [Seminal papers](/references/seminal_papers)（条目：`SP-M2-Khinchin1949`）。
  - 必读导读：了解为什么 Khinchin 认为我们不需要物理上的“各态历经假说”来支撑统计力学。
- **Ramsey 1956**: *Negative absolute temperature*. 导读见 [Seminal papers](/references/seminal_papers)（条目：`SP-M2-Ramsey1956-NegativeTemperature`）。
  - 负温度不是“更冷”，而是 \(\partial S/\partial E<0\) 的平衡态现象；理解它需要明确“能量谱上界”的必要条件。

---

## Part 1：微正则的哲学——真的需要“各态历经”吗？

教科书常说：微正则系综成立的前提是“各态历经假设”（系统随时间演化会跑遍所有可能的微观态）。
这在数学上很难证明，且对于许多真实物理系统（如玻璃态、近可积系统）甚至不成立。

但 Khinchin (1949) 和 Jaynes 告诉我们：只要系统自由度足够大，且我们只关心宏观量（如压强、总磁矩），我们其实不需要“跑遍每一个微观态”。我们需要的只是“**几乎所有微观态**”都对应着相同的宏观值。
这叫 **测度集中**（measure concentration）。
所以，不要纠结于动力学是否真的遍历；只要我们对初始态无知，**等概率原理**（equal a priori probability）就是最诚实的推断基线。

> 👉 **深入理解**：
> - [[微正则系综 Microcanonical ensemble]]：定义与假设。

---

## Part 2：温度的统计定义——熵的斜率

在 M0 热力学中，我们有 \(dU = T\,dS - P\,dV + \mu\,dN\)。把它改写成关于 \(S(E,V,N)\) 的微分形式，并在此处固定 \(V,N\)，就得到：
\[ \frac{1}{T} = \left(\frac{\partial S}{\partial E}\right)_{V,N} \]
在 M2 里，利用 $S = k_B \ln \Omega(E)$，我们终于明白了**温度的统计本质**：
- 温度衡量的是：**当你给系统一点能量 $dE$，它的微观状态数 $\Omega$ 膨胀得有多快**？
- 正常系统（$T>0$）：能量越高，可及态数通常越多（\(\Omega\uparrow\)），因此 \(\left(\partial S/\partial E\right)_{V,N}>0\)。
- 负温度系统（$T<0$）：只可能出现在**能量有上界**的系统里；在高能端 \(S(E)\) 会随 \(E\) 增加而下降（典型例：受磁场约束的自旋系统）。详见：[[负温度 Negative temperature]]。

把“熵的斜率”这句话落到一个真正可算的物理例子：经典理想气体。微观态是相空间点 \((\mathbf r_i,\mathbf p_i)\)，而“计数”不再是组合数，而是相空间体积 \(\Omega(E,V,N)\)。

### 例子：理想气体相空间计数（推导骨架）
为避免连续体系“能量恰好等于 \(E\)”带来的测度/单位麻烦，我们先用一个常见且更好算的“体积型”定义（能量不超过 \(E\) 的相空间体积）来说明标度关系：
\[
\Omega(E,V,N)\propto \int_{V^N} d^{3N}r \int d^{3N}p\;\Theta\!\left(E-\sum_{i=1}^N\frac{\mathbf p_i^2}{2m}\right).
\]
对理想气体 \(H(\mathbf p,\mathbf r)=\sum_i \mathbf p_i^2/(2m)\)，它不依赖 \(\mathbf r\)，所以位置积分直接给出
\[
\int_{V^N} d^{3N}r = V^N.
\]
动量积分的约束
\[
\sum_{i=1}^N\frac{\mathbf p_i^2}{2m}\le E
\]
在 \(3N\) 维动量空间里就是一个半径 \(R=\sqrt{2mE}\) 的球体。因此动量部分的体积满足
\[
\int d^{3N}p\;\Theta(\cdots)\;\propto\;R^{3N}\;\propto\;(2mE)^{3N/2}\;\propto\;E^{3N/2}.
\]
合起来得到最关键的标度：
\[
\Omega(E,V,N)\propto V^N E^{3N/2}.
\]
取对数（把只依赖 \(N,m,h\) 的常数项都并入 “const”）：
\[
S(E,V,N)=k_B\ln\Omega \;=\; k_B\left[N\ln V+\frac{3N}{2}\ln E+\text{const}\right].
\]
现在偏导就不再是“宣告”，而是直接算出来的：
\[
\frac{1}{T}=\left(\frac{\partial S}{\partial E}\right)_{V,N}=k_B\frac{3N}{2}\frac{1}{E}
\quad\Rightarrow\quad
E=\frac{3}{2}Nk_BT,
\]
\[
\frac{P}{T}=\left(\frac{\partial S}{\partial V}\right)_{E,N}=k_B\frac{N}{V}
\quad\Rightarrow\quad
PV=Nk_BT.
\]
更严格的“能量壳/态密度”版本会把 \(E^{3N/2}\) 改成 \(E^{3N/2-1}\)，但在热力学极限下只差一个不影响宏观偏导的次要修正；完整推导与符号说明见：[[理想气体的微正则态数（相空间体积 Ω）]]。

强度量偏导形式的通用推导见：[[T,P,μ 作为熵的偏导定义]]。

---

## Part 3：热力学极限——为什么宏观是确定的？

既然微观是概率的，为什么宏观测量值（如压强）看起来完全没有涨落？
这是因为 $N \sim 10^{23}$。根据大数定律（或中心极限定理），相对涨落 $\frac{\delta X}{X} \sim \frac{1}{\sqrt{N}}$。
对于宏观物体，这个涨落小到 $10^{-11}$ 量级，完全不可测。这就是 Khinchin 强调的**热力学极限**（thermodynamic limit）。

一个最干净的“典型性”例子是两盒分球/硬币模型：宏观态用左盒球数 \(n\) 表示，多重度是 \(\Omega(n)=\binom{N}{n}\)。你经常会看到一张图，把
\[
\ln\binom{N}{n}
\]
在 \(n=N/2\) 附近画成一个近似二次函数，从而得到 \(P(n)\) 近似高斯。那张图其实做了两步：
1) 用 Stirling 近似把 \(\ln\binom{N}{n}\) 写成 \(N H(x)\)（其中 \(x=n/N\)，\(H(x)=-x\ln x-(1-x)\ln(1-x)\) 是二元熵函数）；  
2) 再把 \(H(x)\) 在 \(x^\*=1/2\) 附近二阶展开，得到高斯集中与方差标度。

这条推导链的完整版本（含每一步的衔接句与最终 \(\mathrm{Var}(n)=N/4\) 的对齐验证）见：[[二项系数的熵函数近似与高斯集中（Stirling→H(x)）]]。

这也解释了为什么从微正则（$E$ 固定）转到正则（$E$ 涨落）不会改变宏观测量结果——因为在正则系综里，$E$ 的分布极窄，看起来就像固定在 $\langle E \rangle$ 处一样。
更系统的说法是：正则系综的能量分布在热力学极限下会“尖峰化”（相对涨落 \(\sim 1/\sqrt{N}\)），或者用 \(\Omega(E)e^{-\beta E}\) 的鞍点近似直接把 \(\beta\) 与微正则温度定义对齐。详见：[[微正则与正则的系综等价性（能量尖峰化与鞍点）]]。

> 👉 [[热力学极限 Thermodynamic limit]]：大数定律的胜利。

---

## Part 4：城市映射——封闭系统基线

微正则在城市里的直接应用较少，因为城市总是开放的（M4）。
但它作为一个**基线**（Baseline）非常重要：
- 想象一个完全封闭的小区（Lockdown），总物资 $E$ 固定，总人数 $N$ 固定。
- 此时资源的分配完全由组合数（熵最大）决定。
- 它告诉我们：**如果你不去干预（引入外部势），系统自然会演化到最混乱（公平？）的状态。**

### 一个最小例子：封闭社区的“两区分配”
设一个封闭社区里有 \(N\) 个个体（或 \(N\) 次独立事件），每个个体只能在两种对称选项 \(A/B\) 中做选择（例如：在封闭园区内两处服务点之间选择）。如果我们对“为什么选 \(A\) 或 \(B\)”完全无知，那么最诚实的基线就是：所有 \(2^N\) 种微观选择配置等概率。

令宏观量 \(n\) 表示选择 \(A\) 的个体数，则多重度
\[
\Omega(n)=\binom{N}{n}.
\]
因此最概然值是 \(n^\*=N/2\)，标准差 \(\sigma_n=\sqrt{N/4}\)，所以份额 \(n/N\) 的典型波动尺度是
\[
\frac{\sigma_n}{N}=\frac{1}{2\sqrt{N}}=O\!\left(\frac{1}{\sqrt{N}}\right).
\]
这就是“封闭硬约束 + 等概率计数”在城市里的最小直觉：**当你没有任何偏好信息时，宏观上最常见的是对称、均匀、且涨落随规模变小的状态**。  
一旦你在数据里看到长期偏离（例如稳定的 70/30），这通常意味着系统并非“无偏”：要么存在成本差/吸引力差（会在 M3 变成指数权重），要么系统是开放的、规模可变的（M4 引入 \(\mu\)）。

| 统计物理 | 城市映射 |
| :--- | :--- |
| **能量 $E$** | 总预算 / 总资源 |
| **等概率原理** | 随机分配机制（Lottery） |
| **熵最大化** | 资源最分散的分布（最平庸的分布） |

> 👉 [[微正则（闭系统）→ 城市资源约束基线]]

---

## 自检问题
1. 为什么说在大系统里，我们往往不需要把“各态历经假设”当作公理？（提示：典型性/测度集中）
2. 从 \(S=k_B\ln\Omega\) 推出 \(1/T=\left(\partial S/\partial E\right)_{V,N}\) 需要什么物理设定？（提示：两系统热接触、总能量守恒）
3. 什么条件下会出现负温度？负温度比正温度“更热”还是“更冷”？为什么？
4. 为什么微正则与正则在热力学极限下会给出相同的宏观预测？（提示：能量分布尖峰化/鞍点）

---

## 通往下一章（M3）
- M2 的硬约束在真实系统里难以维持；与热库交换后，能量“允许波动”，正则系综成为主力工具。
- M3 会引入配分函数 \(Z\)，把“难算的几何计数”变成“可导的生成函数”。

## Part 5：动手时刻 (Checklist)

### 必读
- [ ] **Reading Guide**: [[Khinchin_Foundations_Intro]] (Introduction)
  > 点击查看为什么“各态历经”可能是一个伪问题。

### 习题
- [ ] **Written**: `exercises/written/M2_microcanonical_basics.md`
  - 核心任务：推导理想气体的 \(\Omega(E,V,N)\sim V^N E^{3N/2}\)，并验证 \(PV=Nk_B T\)。
  
### 验收标准
- [ ] 能够写出 $S = k_B \ln \Omega$ 并解释 $\Omega$ 的含义。
- [ ] 能够从 $\Omega(E)$ 求出温度 $T(E)$。
- [ ] 知道为什么在 $N \to \infty$ 时，微正则和正则系综给出的热力学预测是一样的（系综等价性；见：[[微正则与正则的系综等价性（能量尖峰化与鞍点）]]）。
